CREATE TABLE dogs(
    name TEXT,
    breed TEXT,
    age INTEGER
);

CREATE TABLE cats(
    name TEXT,
    breed TEXT,
    age INTEGER
);

.open dogs_db.db  -- this will save the database, as all the actions are ephemeral