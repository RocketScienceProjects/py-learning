# Iterable: A obj that returns an iterator when "iter()" function is called upon
# Iterator: A obj that can be iterated upon and returns data when the func "next()" is called upon

# A string "apple" is iterable but not an iterator
# iter("apple") -> returns an iterator